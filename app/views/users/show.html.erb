

<h1>Tweets</h1>

<div id="show">
  <table>
    <tbody>
      <tr v-for="tweet in tweets">
        <td>{{ tweet.user.username }} </a></td>
        <td>{{ tweet.follow }}</td>
        <td>{{ tweet.content }}</td>
      </tr>
    </tbody>
  </table>
 </div>

</div>
